# builder = Dockerfile + ENTRYPOINT (docker-entrypoint.sh). Vacía cualquier Start Command manual en Railway.
[build]
builder = "DOCKERFILE"

[deploy]
# Si el proceso cae (DB momentánea, SIGKILL en redeploy), Railway lo reintenta.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 15

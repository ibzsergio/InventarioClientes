# Solo fuerza Docker. Sin healthcheckPath: el check mataba el deploy si migrate tarda o el proxy aún no enruta.
# Sin startCommand: usa ENTRYPOINT del Dockerfile (PORT y migrate resueltos ahí).
# Postgres: en el servicio web, referencia DATABASE_URL del plugin.
[build]
builder = "DOCKERFILE"
